import React from 'react';

function App() {
  return (
    <div style={{ textAlign: 'center', marginTop: '3rem' }}>
      <h1>React Dockerized App</h1>
      <p>This React app is running inside a Docker container using Nginx.</p>
    </div>
  );
}

export default App;
